<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />

    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
      integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm"
      crossorigin="anonymous"
    />
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet">

    <title>Products</title>
  </head>
  <body>
    <%- include('../components/sidenav') %>
    <div style="margin-left: 200px;">
        <%- include('../components/breadcrumb', { menus: [{ name: 'Products', link: '/' }] }) %>
        <div class="container">
          <h1>Products</h1>
          <div class="d-flex justify-content-end">
            <a href="/products/add">
              <button type="button" class="btn btn-success">Add Product</button>
            </a>
          </div>

          <% dataShow = data.map( (res, index) => {
            return {
              id: res.id,
              no: index + 1,
              'nama produk': res.name,
              harga: rupiah(res.price),
              stock: numberSeparator(res.stock),
              action: [
            { name: 'Lihat Detail', link: '/products/:id'},
            { name: 'Edit', link: '/products/:id/edit'},
            { name: 'Hapus', link: '/products/:id/delete'}
          ]
            }
          }) %>

          <%- include('../components/table', { data: dataShow }) %>
        </div>
    </div>

    <!-- Optional JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    <script
      src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js"
      integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q"
      crossorigin="anonymous"
    ></script>
    <script
      src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"
      integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
